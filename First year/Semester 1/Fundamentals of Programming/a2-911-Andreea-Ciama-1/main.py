import math


def sinus(x):
    """
    Computes sin(x) forr a given real number x
    :param x:  double,eps double
    :return: rez- double, an approximation to the value of sin(x) with eps error
    """

    s=0.0

    return 1.0


def test_sinus():
    x = 0
    eps = 0.0001
    assert (abs(sinus(x) - math.sin(x)))
